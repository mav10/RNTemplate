import { StyleSheet } from 'react-native';

export const localStyles = StyleSheet.create({
  safeArea: { flex: 1, justifyContent: 'space-between', alignItems: 'stretch' },
  container: { alignItems: 'center' },
  footer: { alignItems: 'center', gap: 5 },
});
